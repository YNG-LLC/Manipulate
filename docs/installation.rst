============
Installation
============

At the command line::

    $ pip install YNG-Manipulate

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv YNG-Manipulate
    $ pip install YNG-Manipulate
